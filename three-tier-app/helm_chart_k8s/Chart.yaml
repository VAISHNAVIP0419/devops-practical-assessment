# Helm Chart metadata for Three-Tier MERN Application
apiVersion: v2
name: helm_chart_k8s
description: Production-ready Helm chart for three-tier MERN application on Kubernetes

# Application chart - packages templates for deployment
type: application

# Chart version - increment on template/configuration changes
version: 0.1.0

# Application version - version of the three-tier app being deployed
appVersion: "1.16.0"
